# ================================================================
# Main config.fish
# ================================================================

# All heavy lifting is now in conf.d/ files (loaded automatically)
# Add only things that must run last if needed
